import { OrderNamesPipe } from './order-names.pipe';

describe('OrderNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
