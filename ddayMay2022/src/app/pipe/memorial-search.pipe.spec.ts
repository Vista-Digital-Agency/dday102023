import { MemorialSearchPipe } from './memorial-search.pipe';

describe('MemorialSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MemorialSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
